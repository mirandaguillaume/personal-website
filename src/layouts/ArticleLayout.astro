---
import BaseLayout from './BaseLayout.astro';
import TagPill from '../components/TagPill.astro';
import type { Lang } from '../i18n/ui';
import { articleAlternatePath, otherLang } from '../i18n/alternates';
import type { CollectionEntry } from 'astro:content';
import { render } from 'astro:content';

interface Props {
  post: CollectionEntry<'blogFr'> | CollectionEntry<'blogEn'>;
  lang: Lang;
}

const { post, lang } = Astro.props;
const { Content } = await render(post);
// null plutôt que undefined : dit explicitement à BaseLayout « pas de
// traduction publiée », au lieu de le laisser deviner par le chemin.
const alternate =
  (await articleAlternatePath(post.data.translationKey, otherLang(lang))) ??
  null;
const formattedDate = post.data.pubDate.toLocaleDateString(
  lang === 'fr' ? 'fr-FR' : 'en-US',
  {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  },
);
const tagsBase = lang === 'fr' ? '/blog/tags' : '/en/blog/tags';
---

<BaseLayout
  lang={lang}
  title={post.data.title}
  description={post.data.description}
  alternate={alternate}
  ogType="article"
>
  <article>
    {
      post.data.draft && (
        <p class="draft-badge">{lang === 'fr' ? 'Brouillon' : 'Draft'}</p>
      )
    }
    <p class="article-card__date">{formattedDate}</p>
    <h1>{post.data.title}</h1>
    <div>
      {
        post.data.tags.map((tag) => (
          <TagPill tag={tag} href={`${tagsBase}/${tag}/`} />
        ))
      }
    </div>
    <Content />
  </article>
</BaseLayout>
